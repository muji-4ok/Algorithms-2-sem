//
// Created by egork on 19.03.2020.
//
#include "gtest/gtest.h"
#include "BigInteger.h"

TEST(BigInt_Add, Test1) {

}

TEST(BigInt_Multiply, Test1) {

}

TEST(BigInt_Divide, Test1) {

}

TEST(BigInt_toString, Test1) {

}

TEST(BigInt_IncrementsDecrements, Test1) {

}

TEST(BigInt_Relations, Test1) {

}

//TEST(ATest, CorrectSize) {
//    ASSERT_EQ(sizeof(smallPrimes) / sizeof(int), 168);
//}

/*
TEST(ATest, BeginningPrimes) {
    EXPECT_EQ(smallPrimes[0], 2);
    EXPECT_EQ(smallPrimes[1], 3);
    EXPECT_EQ(smallPrimes[2], 5);
    EXPECT_EQ(smallPrimes[3], 7);
    EXPECT_EQ(smallPrimes[4], 11);
}

TEST(ATest, LastPrime) {
    EXPECT_EQ(smallPrimes[168], 997);
}
*/

/*
TEST(BTest, AdditionBasic) {
    ASSERT_EQ(safeAdd(1, 1), 2);
    EXPECT_EQ(safeAdd(1 << 7, 1), (1 << 7) + 1);
    EXPECT_EQ(safeAdd(-2, 2), 0);
    EXPECT_EQ(safeAdd(-3, -5), -8);
    EXPECT_EQ(safeAdd(1, -4), -3);
}
*/

//TEST(BTest, AdditionOverflow) {
//    ASSERT_THROW(safeAdd(std::numeric_limits<int>::max(), 5), std::runtime_error);
//    ASSERT_THROW(safeAdd(std::numeric_limits<int>::min(), -5), std::runtime_error);
//}

/*
TEST(BTest, SubtractionBasic) {
    ASSERT_EQ(safeSubtract(1, 1), 0);
    EXPECT_EQ(safeSubtract(1 << 7, 1), (1 << 7) - 1);
    EXPECT_EQ(safeSubtract(-2, 2), -4);
    EXPECT_EQ(safeSubtract(-3, -5), 2);
    EXPECT_EQ(safeSubtract(1, -4), 5);
}

TEST(BTest, SubtractionOverflow) {
    ASSERT_THROW(safeSubtract(std::numeric_limits<int>::max(), -5), std::runtime_error);
    ASSERT_THROW(safeSubtract(std::numeric_limits<int>::min(), 5), std::runtime_error);
}
*/
